#!/usr/bin/env sh
self="$0"
while [ -L "$self" ]; do
  dir=$(cd "$(dirname "$self")" && pwd)
  self=$(readlink "$self")
  case "$self" in /*) ;; *) self="$dir/$self" ;; esac
done
basedir=$(cd "$(dirname "$self")" && pwd)
exec "$basedir/../vendor/quest" "$@"
